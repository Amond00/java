package sec1;

import s2.

public class Memeber {
	/*클래스 member의 속성(field)
	public 현재 프로젝트 내에서 다른 패키지에서도 접근 가능
	default 접근 제한자를 생략했을때, 같은 패키지 내에서 접근 가능
	private 현재 클래스 내에서만 접근 가능 */
	private String id;	
	private String password;
	private String name;
	private String adress;
	private String tel;
	private String reg_date;
	
	
}